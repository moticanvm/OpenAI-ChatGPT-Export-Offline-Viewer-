# OpenAI-ChatGPT-Export-Offline-Viewer-
Offline ChatGPT export viewer. Single HTML file — no Python, no dependencies, no install. Open in browser, select your exported folder. View conversations with images, audio playback, and search. Export individual or all chats as PDF, HTML, Markdown, or RTF. No external calls, everything runs locally.
